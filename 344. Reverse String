class Solution {
    public void reverseString(char[] s) {
        int len=s.length;
        char arr[]=new char[len];
        for(int i=0;i<len;i++){
            arr[i]=s[len-i-1];
        }
        for(int i=0;i<len;i++){
            s[i]=arr[i];
        }
    }
}
